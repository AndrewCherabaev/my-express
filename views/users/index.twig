{% extends 'layout.twig' %}

{% block body %}
    <h1>{{ title }}</h1>
    <ul>
        {% for user in users %}
            <li><a href="/users/{{ user.id }}">{{ user.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
